namespace BenheimQoL.WorldLabels;

// The shared shortcut catalog still references this value on the base commit.
// The Pine checkpoint removes that stale copy and this compatibility shim.
internal static class WorldLabelVisibility
{
    internal const float PortalMaxDistanceMeters = 30f;
}
